Compare strategies side-by-side. User input: $ARGUMENTS

Parse input for strategy names. Default: `gods_plan steady`
If $ARGUMENTS has two strategy names, use those.

Run `uv run diamond compare <strategy1> <strategy2>`

After showing the comparison, provide commentary:
- Which strategy is outperforming and why
- Risk-adjusted returns (Sharpe-like comparison)
- Sector overlap — are they diversified from each other?
- Recommendation: stick with current allocation or shift weight?