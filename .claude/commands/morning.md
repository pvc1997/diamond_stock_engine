Pre-market morning ritual. Run all of these and present a crisp briefing:

1. Run `uv run diamond streak` — log daily check-in first
2. Run `uv run diamond pulse` — market verdict
3. Run `uv run diamond today` — daily briefing
4. Run `uv run diamond watch` — watchlist alerts

Present as a quick-scan morning briefing:
- **Market**: One-line verdict (DEPLOY/WAIT/DEFENSIVE) + Nifty + VIX
- **Portfolio**: NAV + day change + any alerts
- **Watchlist**: Any triggered price alerts or RSI signals
- **Streak**: Check-in status
- **One Thing**: The single most important action for today (or "All clear")

Keep this SHORT — under 20 lines. This is the "glance at your phone" version. Use `/daily` for the full deep briefing.